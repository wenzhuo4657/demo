server:
  port: 9090
spring:
  application:
    name: spring-cloud-test
  config:
      import:
        - optional:configserver:http://127.0.0.1:8080

  profiles:
    active: dev
  cloud:
    config:
      label: main

logging:
  level:
    org.springframework.cloud.config.server: DEBUG

