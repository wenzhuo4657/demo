spring:
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wenzhuo4657/springcloudconfig-test.git #配置文件所在仓库
          username: wenzhuo4657
          password: ${MY_PASSWORD}
          default-label: main #配置文件分支
          search-paths:   #配置文件所在根目录

#        todo  读取超时，后续使用gitee做测试，或者尝试